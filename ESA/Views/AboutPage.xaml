<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ESA.Views.AboutPage" BackgroundColor="#222222" Title="About Us" >
    <ContentPage.Content>
        <ListView x:Name="ListViewAbout" ItemsSource="{Binding People}" HasUnevenRows="True" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell IsEnabled="False">
                        <ViewCell.View>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="150"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <StackLayout Grid.Row="0" Orientation="Horizontal" Margin="5" HorizontalOptions="Center">
                                    <Image Source="{Binding Image}" WidthRequest="150" HeightRequest="150"/>
                                </StackLayout>
                                <StackLayout Grid.Row="1" Orientation="Vertical">
                                    <Label Text="{Binding PersonName}" FontSize="20" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center"/>
                                    <Label Text="{Binding ShortDesc}" FontSize="14" TextColor="LightGray" Margin="10,0,10,0" HorizontalTextAlignment="Center" />
                                    <BoxView HeightRequest="1" Color="Black" />
                                </StackLayout>
                            </Grid>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>